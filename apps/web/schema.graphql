# Auto-generated schema from API gateway code-first resolvers.
# This file is used by GraphQL Codegen when the gateway is not running.
# To regenerate: start the gateway and run introspection, or copy from
# apps/api-gateway/schema.graphql

type UserProfile {
  userId: String!
  email: String!
  name: String
  roles: [String!]!
}

type Item {
  id: ID!
  name: String!
  description: String
  createdAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type ItemsResponse {
  items: [Item!]!
  total: Float!
}

type Query {
  health: String!
  me: UserProfile!
  getItems(page: Float! = 1, limit: Float! = 10): ItemsResponse!
  getItem(id: String!): Item
}

type Mutation {
  createItem(name: String!, description: String): Item!
}
